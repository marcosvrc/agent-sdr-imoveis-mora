# Base da API do CRM. NUNCA coloque token de serviço aqui: o painel usa sessão em cookie, e
# qualquer variável VITE_ vai para dentro do bundle que o navegador baixa.
VITE_CRM_API=http://localhost:8100
