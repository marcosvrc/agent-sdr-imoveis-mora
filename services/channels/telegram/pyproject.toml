[project]
name = "sdr-channel-telegram"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["sdr-shared", "httpx>=0.27"]
[project.optional-dependencies]
[tool.uv.sources]
sdr-shared = { path = "../../../shared", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["canal_telegram"]
